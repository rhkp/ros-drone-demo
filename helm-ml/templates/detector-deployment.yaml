{{- $detector := .Values.detector | default dict }}
{{- if (default false $detector.enabled) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "farm-drone-ml-data.fullname" . }}-detector
  labels:
    {{- include "farm-drone-ml-data.labels" . | nindent 4 }}
    app.kubernetes.io/component: camera-detector
spec:
  replicas: 1
  # The detector reserves the cluster's single GPU; release the old pod before
  # starting a new image so upgrades do not deadlock in Pending.
  strategy:
    type: Recreate
    rollingUpdate: null
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "farm-drone-ml-data.name" . }}-detector
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "farm-drone-ml-data.name" . }}-detector
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: camera-detector
    spec:
      securityContext:
        runAsNonRoot: true
      {{- with ((.Values.placement | default dict).nodeSelector) }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with ((.Values.placement | default dict).tolerations) }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/name
                    operator: In
                    values:
                      - {{ include "farm-drone-ml-data.name" . }}-viewer
                      - {{ include "farm-drone-ml-data.name" . }}-storage-anchor
              topologyKey: kubernetes.io/hostname
      containers:
        - name: detector
          image: {{ default "quay.io/rhkp/hbr-drone-observer:v0.9.0" $detector.image | quote }}
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-lc"]
          args:
            - >-
              source /opt/drone/scripts/ros-env.sh &&
              source /opt/drone_ws/install/setup.bash &&
              exec ros2 run drone_observer vision_detector
          env:
            - name: RMW_IMPLEMENTATION
              value: rmw_zenoh_cpp
            - name: ZENOH_ROUTER_ENDPOINT
              value: {{ default "tcp/farm-drone-ros-drone-demo-zenoh.arhkp1-farm-drone.svc.cluster.local:7447" $detector.zenohEndpoint | quote }}
            - name: DRONE_MODEL_PATH
              value: {{ default "/data/perception/models/v7/detector.pt" $detector.modelPath | quote }}
            - name: DRONE_MODEL_VERSION
              value: {{ default "v7" $detector.modelVersion | quote }}
            - name: DRONE_CLASS_NAMES
              value: {{ default "well,storage_silo,cattle_shed,produce_barn,crop_field,greenhouse,water_tank" $detector.classNames | quote }}
            - name: DRONE_CONFIDENCE_THRESHOLD
              value: {{ default "0.35" $detector.confidenceThreshold | quote }}
            - name: DRONE_DETECTION_TOPIC
              value: {{ default "/drone/camera_detections" $detector.outputTopic | quote }}
          resources:
            requests:
              cpu: {{ default "500m" $detector.cpuRequest | quote }}
              memory: {{ default "2Gi" $detector.memoryRequest | quote }}
              nvidia.com/gpu: {{ default "1" $detector.gpuRequest | quote }}
            limits:
              cpu: {{ default "2" $detector.cpuLimit | quote }}
              memory: {{ default "4Gi" $detector.memoryLimit | quote }}
              nvidia.com/gpu: {{ default "1" $detector.gpuLimit | quote }}
          volumeMounts:
            - name: perception-data
              mountPath: {{ default "/data/perception" .Values.dataPath | quote }}
              readOnly: true
      volumes:
        - name: perception-data
          persistentVolumeClaim:
            claimName: {{ include "farm-drone-ml-data.fullname" . }}
{{- end }}
